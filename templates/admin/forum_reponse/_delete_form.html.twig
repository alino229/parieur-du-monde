<form method="post" action="{{ path('forum_reponse_delete', {'id': forum_reponse.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ forum_reponse.id) }}">
    <button class="btn">Delete</button>
</form>
