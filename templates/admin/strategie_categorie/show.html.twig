{% extends 'base.html.twig' %}

{% block title %}StrategieCategorie{% endblock %}

{% block body %}
    <h1>StrategieCategorie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ strategie_categorie.id }}</td>
            </tr>
            <tr>
                <th>Categorie</th>
                <td>{{ strategie_categorie.categorie }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('strategie_categorie_index') }}">back to list</a>

    <a href="{{ path('strategie_categorie_edit', {'id': strategie_categorie.id}) }}">edit</a>

    {{ include('admin/strategie_categorie/_delete_form.html.twig') }}
{% endblock %}
