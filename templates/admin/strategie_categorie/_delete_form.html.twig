<form method="post" action="{{ path('strategie_categorie_delete', {'id': strategie_categorie.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ strategie_categorie.id) }}">
    <button class="btn">Delete</button>
</form>
