{% extends 'admin_base.html.twig' %}

{% block title %}Article index{% endblock %}

{% block body %}
    <section class="content-header">
        <div class="container-fluid">
            <div class="row mb-2">
                <div class="col-sm-6">
                    <h1>Project Add</h1>
                </div>
                <div class="col-sm-6">
                    <div class="breadcrumb float-sm-right">
                        <button type="button" class="btn btn-tool" data-card-widget="collapse" data-toggle="tooltip" title="Collapse">
                            <i class="fa fa-minus">Create new Article</i>
                        </button>
                    </div>
                </div>
            </div>
        </div><!-- /.container-fluid -->
    </section>
    <section class="content">
        <div class="row">
            <div class="col-md-12">
                <div class="card card-primary">

                    <div class="card-body">
                        <div class="form-group">
                            <label for="inputName">Identifient  </label>
                            <input class="form-control" type="text" placeholder="{{ role.id }}" readonly>


                        </div>
                        <div class="form-group">
                            <label for="inputName">Role</label>
                            <input class="form-control" type="text" placeholder="{{ role.type }}" readonly>


                        </div>
                        <div class="form-group">
                            <label for="inputName">Role</label>
                            <input class="form-control" type="text" placeholder="{{ role.UserPseudo[0] }}" readonly>


                        </div>


                    </div>
                    <!-- /.card-body -->
                </div>
                <!-- /.card -->
            </div>

        </div>


        <div class="card-footer">
            <a class="btn btn-info btn-sm" href="{{ path('role_index') }}">retour à la liste</a>

            <a class=" btn btn-info btn-sm " href="{{ path('role_edit', {'id': role.id}) }}">éditer</a>

            {{ include('admin/role/_delete_form.html.twig') }}
        </div>
    </section>

{% endblock %}
