{% extends 'admin_base.html.twig' %}

{% block title %}Article index{% endblock %}

{% block body %}
    <h1>CategoriePronostics index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Type</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for categorie_pronostic in categorie_pronostics %}
            <tr>
                <td>{{ categorie_pronostic.id }}</td>
                <td>{{ categorie_pronostic.type }}</td>
                <td>
                    <a href="{{ path('categorie_pronostics_show', {'id': categorie_pronostic.id}) }}">show</a>
                    <a href="{{ path('categorie_pronostics_edit', {'id': categorie_pronostic.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('categorie_pronostics_new') }}">Create new</a>
{% endblock %}
