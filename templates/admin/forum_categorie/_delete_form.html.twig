<form method="post" action="{{ path('forum_categorie_delete', {'id': forum_categorie.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ forum_categorie.id) }}">
    <button class="btn">Delete</button>
</form>
