{% extends "base.html.twig"%}
{% block title %}Index{% endblock %}
{% block head %}
    {{ parent() }}

{% endblock %}
{% block body %}
    <section id="contact">
        <div class="" style="margin: 0 auto;">
            {% if msg_succes %}
                <span class="msg-succes">{{ msg_succes }}</span>
            {% endif %}

            <div class="forum_cont signin-container">
                <div class="sign_body">
                    <div class="form-header" style="margin-bottom: 3rem">
                        <h2>Contact</h2>
                    </div>
                    <form class="contact-form" method="post"  style="padding-top: 3rem">

                        <div class="form-group">
                            <div class="row">
                                <div class="col-1-of-2 mb0 i-mb0">
                                    <div class="form-label-group">
                                        <input type="text" name="name" class="Inputname form-control" placeholder="votre nom" required >
                                        <span><?php if(isset($erreur_name)) echo $erreur_name;?></span>
                                    </div>
                                </div>
                                <div class="col-1-of-2">
                                    <div class="form-label-group">
                                        <input type="email" name="email" class="Inputname form-control" placeholder="votre email" required>
                                        <span><?php if(isset($erreur_email)) echo $erreur_email;?></span>

                                    </div>
                                </div>
                            </div>
                        </div>
                        <div class="form-group">
                            <div class="row">
                                <div class="form-label-group">
                                    <input type="text" name="webSite" placeholder="votre site web" class="form-control">
                                    <span style="color: red;font-size: 1.3rem"><?php if(isset($erreur_sujet)) echo $erreur_sujet;?></span>

                                </div>

                            </div>
                        </div>
                        <div class="form-group">
                            <div class="row">

                                <div class="form-group">
                                    <textarea name="message" class="form-control" id="message"  placeholder="Votre  message" required></textarea>
                                    <span style="color: red;font-size: 1.3rem"><?php if(isset($erreur_message)) echo $erreur_message;?></span>

                                </div>
                            </div>
                        </div>
                        <div class="form-group">
                            <div class="row botton ">
                                <input type="submit" name="contact" value="envoyer" class="btn signin-btn btn-lg">
                            </div>
                        </div>
                    </form>

                </div>
            </div>


        </div>
    </section>

{% endblock %}