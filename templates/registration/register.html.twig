{% extends "base.html.twig"%}
{% block title %}inscription{% endblock %}
{% block head %}
    {{ parent() }}

{% endblock %}
{% block body %}

    <section  id="contact connect">
        <div class="" style="margin: 0 auto;padding-bottom: 2rem;">
            <div class="forum_cont signin-container sign-container">
                <div class="sign_body body-connect">
                    <div class="form-header" style="margin-bottom: 3rem">
                        <h2>Inscription</h2>
                    </div>
                    <div class="contact-form" >
                        {% for flashError in app.flashes('verify_email_error') %}
                            <div class="alert alert-danger" role="alert">{{ flashError }}</div>
                        {% endfor %}
                        {{ form_start(registrationForm) }}

                        <input type="hidden" name="MAX_FILE_SIZE" value="122880" />

                        <div class="form-group">
                            <div class="row">
                                <div class="form-label-group">
                                     {{ form_row(registrationForm.pseudo) }}</div>
                            </div>
                        </div>
                        <div class="form-group">
                            <div class="row">
                                <div class="form-label-group">
                                    {{ form_row(registrationForm.email) }}

                                </div>
                            </div>
                        </div>
                       {# <div class="form-group">
                            <div class="row">
                                <div class="form-label-group">
                                    <input type="password" class="form-control" name="password" id="passwordInput" placeholder="Password" required>
                                    <span class="erreur" id="js-password_erreur" style="display: none"></span>
                                    <span class="{% if erreur_password  %}erreur{% endif %}">{% if erreur_password %}{{ erreur_password }}{% endif %}</span>
                                    <label for="passwordInput">&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;</label>

                                </div>
                                <!---->

                            </div>
                        </div>#}
                        <div class="form-group">
                            <div class="row">

                                <div class="form-label-group">
                                    {{ form_row(registrationForm.plainPassword, {
                                        label: 'Password'
                                    }) }}

                                </div>
                            </div>
                        </div>
                      {#  <div class="form-group" style="margin-bottom: 0!important;">
                            <div class="row">
                                <div class="form-check" style="color: #f6f6f6;">
                                    <label class="form-check-label" for="avatar"></label>
                                    <span style="font-size: 1.2rem;padding: .5rem;display: inline-block">Choisissez votre avatar (Taille max : 120Ko):</span><br/>
                                    <input type="file" name="avatar" id="avatar" value="{{ uavatar }}" accept="image/jpeg"/><br />
                                    <span class="{% if erreur_avatar %}erreur{% endif %}">{% if erreur_avatar %} {% for key in erreur_avatar %} {{ key }}{% endfor %}{% endif %}</span>

                                </div>
                            </div>
                        </div>#}

                        <div class="form-group">
                            <div class="row botton ">
                                <input type="submit" name="inscription_submit" class="btn signin-btn btn-lg" id="sign-validate" value="s'inscrire">

                            </div>
                        </div>
                        {{ form_end(registrationForm) }}
                    </div>
                    <div class="text_center">
                        <div class="politiq">
                            En cliquant sur <b>«s'inscrire»</b>, vous acceptez nos<br/>
                            <p><a href="{{ path('confidentialite') }}">conditions d'utilisation</a></p>
                            {#<pre></pre>#}
                        </div>

                    </div>

                </div>
            </div>


        </div>


    </section>

{% endblock %}
